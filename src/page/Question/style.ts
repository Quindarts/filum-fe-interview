import styled from "@emotion/styled";
import { Box } from "@mui/material";

const CustomQuestion = styled(Box)`
  display: flex;
  flex-direction: column;
  align-items: center;
  height: 100vh;
  margin-left: auto;
  margin-right: auto;
  width: 100vw;
`;
export default CustomQuestion;
